---
title: Shows
---

<div class="all-shows-container">        
    @{
        var allShows = Documents["AllShows"]
            .Where(show => show.Get<IEnumerable<DateTime>>("showtimes").Any())
            .OrderBy(show => show.Get<IEnumerable<DateTime>>("showtimes").OrderBy(x => x).FirstOrDefault())
            .GroupBy(show => show.Get<IEnumerable<DateTime>>("showtimes").OrderBy(x => x).First().Year)
            .OrderByDescending(grouping => grouping.Key);
        foreach(var grouping in allShows)
        {
            <div class="banner">
                <div class="container">
                    <h1>@grouping.Key</h1>
                </div>
            </div>
            
            <div class="container">
                <div class="row">   
                    @foreach(var show in grouping) 
                    {
                        Html.RenderPartial("Partials/_ShowArchiveCard", show);
                    }
                </div>
            </div>
        }
    }        
</div>

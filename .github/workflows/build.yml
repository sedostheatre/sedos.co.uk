on:
  push:
    branches-ignore:
    - master

jobs:
  build:
    name: Build
    runs-on: windows-2019
    steps:
    - uses: actions/checkout@v2.1.0
    - name: Build
      uses: ecampidoglio/cake-action@v1
      with:
        target: Build
        cake-version: 0.33.0

    
